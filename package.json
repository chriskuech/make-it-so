{
  "name": "@datumsource/make-it-so",
  "version": "0.0.0",
  "description": "Strongly typed and declarative system for modeling complex requirements and setting them idempotently into their desired state.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "check": "tsc --noEmit",
    "prebuild": "npm run clean",
    "build": "tsc",
    "test": "npx ts-node ./tests/functional.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chriskuech/make-it-so.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.15.21",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "tslib": "^2.8.1",
    "typescript": "^5.8.3"
  }
}
