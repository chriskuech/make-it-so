{
  "name": "@datumsource/make-it-so",
  "version": "0.0.0",
  "description": "Strongly typed and declarative system for modeling complex requirements and setting them idempotently into their desired state.",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "build": "tsc",
    "test": "npx ts-node ./tests/functional.ts"
  },
  "publishConfig": {
    "@datumsource:registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/datumsource/make-it-so.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.11.7",
    "rimraf": "^3.0.0",
    "ts-node": "^10.4.0",
    "tslib": "^2.3.1",
    "typescript": "^4.4.4"
  }
}
